Index: main.cpp
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>#include <iostream>\r\n#include <stdio.h>\r\n\r\n// можно запихнуть расчёт ежемесячного платежа в отдельную процедуру, вывод сделать в форме массива, сделать бул как счетчик\r\nint main() {\r\n    int beginning=1000000; // начальный капитал\r\n    int beginning_contribution=300000; // начальная выплата по ипотеке\r\n    int flat=6000000; // стоимость квартиры\r\n    int income =150000; // доходы\r\n    int expenses = 10000;  // расходы Боба\r\n    int expenses2 = 40000; // расходы Алисы\r\n    int time=20*12; // время на погашение ипотеки\r\n    int a=beginning -beginning_contribution; // остаток Боба\r\n    int b=beginning;\r\n    int Bob, Alice;\r\n    int monthly_payment= ((flat +flat*0.08)/ (time)) ; // стоим. кв. плюс проценты по ипотеке и поделим на месяцы\r\n    int k=1, f=0; // вспомогательный счётчик\r\n    printf( \"Bob's calculations \\n\");\r\n    for (int i=1; i<=time; i++) { // расчёт идёт по месяцам\r\n        Bob = (a+(income)-(expenses) -(monthly_payment));\r\n        a= Bob;\r\n        if ( i > 3*12) {\r\n            Bob= a*1.08;\r\n        }                        // после трёх лет процент на вклад увеличился\r\n        else {\r\n            Bob=a*1.06;\r\n        }                         // до трёх лет\r\n        //printf (\"Year%d  Bob= %d\\n\", k, Bob );\r\n        if ( i/12==k) {   // результат выводится по прошедствию года\r\n             printf (\"                                  Year%d  Bob's Capital= %d\\n\", k, Bob );\r\n            k =k+1;\r\n        }\r\n    }\r\n    k=1;\r\n    printf( \"Alice's calculations \\n\");\r\n    for (int j=1; j<=time; j++) {\r\n        Alice = (b  + (income ) - (expenses2));\r\n        b=Alice;\r\n        if (j > 3*12) {\r\n            Alice = (b*1.08);\r\n        } else {\r\n            Alice = (b* 1.06);\r\n        }\r\n       // printf (\" Alice= %d\\n\", b );\r\n        if (j / 12 == k) {   // результат выводится по прошедствию года\r\n            printf(\"                                  Year%d  Alice's Capital= %d\\n\", k, Alice);\r\n            k = k + 1;\r\n            flat = flat *1.05; // каждый год квартира дорожает на 5%\r\n        }\r\n        if (Alice>= flat and f==0  ) {\r\n            f=k;\r\n        }\r\n    }\r\n    if (Bob >0){\r\n        printf (\"Bob paid off the mortgage \\n\");\r\n    }\r\n    printf (\"Alice bought an apartment for %d years \\n\", f);\r\n    printf (\"Bob's Final Capital= %d\\n\", Bob);\r\n    printf (\"Alice's Final Capitale= %d\\n\", Alice );\r\n    return 0;\r\n}\r\n\r\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/main.cpp b/main.cpp
--- a/main.cpp	(revision f6f04dee876651045b536cb5131574fabc27e2aa)
+++ b/main.cpp	(date 1663791475807)
@@ -1,5 +1,46 @@
 #include <iostream>
 #include <stdio.h>
+ int monthly_payment (int beginning,int beginning_contribution,int flat, int time, double intpercent_mortgages ) {
+     int residue = beginning - beginning_contribution;
+     int monthly_payment = ((flat + flat * percent_mortgages)/ (time));
+     return monthly_payment;
+ }
+  int mortgage_costs_and_contributions (int income,int expenses,int residue,int monthly_payment,
+                                        double deposit_interest, int time ) {
+    int numbers[time];
+      if (monthly_payment != 0) {
+          int a = residue;
+          int k=0, f=1;
+          for (int i = 1; i <= time; i++) {
+              if (i>12*3 and k==0 ) {
+                  deposit_interest = deposit_interest * 1.02;
+                  k = 1;
+              }
+              residue = a + income - expenses - monthly_payment;
+              a = residue * deposit_interest;
+              if (i/12==f ) {
+                  numbers[f]=a;
+                  f=f+1;
+              }
+          }
+
+      } else {
+          for (int j = 1; j <= time; j++) {
+              if (i>12*3 and k==0 ) {
+                  deposit_interest = deposit_interest * 1.02;
+                  k = 1;
+              }
+              residue = (a + income - expenses);
+              a = residue * deposit_interest;
+              if (i/12==f ) {
+                  numbers[f]=a;
+                  f=f+1;
+          }
+      }
+      return numbers;
+  }
+
+
 
 // можно запихнуть расчёт ежемесячного платежа в отдельную процедуру, вывод сделать в форме массива, сделать бул как счетчик
 int main() {
@@ -13,50 +54,51 @@
     int a=beginning -beginning_contribution; // остаток Боба
     int b=beginning;
     int Bob, Alice;
-    int monthly_payment= ((flat +flat*0.08)/ (time)) ; // стоим. кв. плюс проценты по ипотеке и поделим на месяцы
+    //int monthly_payment= ((flat +flat*0.08)/ (time)) ; // стоим. кв. плюс проценты по ипотеке и поделим на месяцы
     int k=1, f=0; // вспомогательный счётчик
     printf( "Bob's calculations \n");
-    for (int i=1; i<=time; i++) { // расчёт идёт по месяцам
-        Bob = (a+(income)-(expenses) -(monthly_payment));
-        a= Bob;
-        if ( i > 3*12) {
-            Bob= a*1.08;
-        }                        // после трёх лет процент на вклад увеличился
-        else {
-            Bob=a*1.06;
-        }                         // до трёх лет
-        //printf ("Year%d  Bob= %d\n", k, Bob );
+   // for (int i=1; i<=time; i++) { // расчёт идёт по месяцам
+        //Bob = (a+(income)-(expenses) -(monthly_payment));
+       // a= Bob;
+       // if ( i > 3*12) {
+        //    Bob= a*1.08;
+       // }                        // после трёх лет процент на вклад увеличился
+       // else {
+        //    Bob=a*1.06;
+      //  }                         // до трёх лет
+      //  //printf ("Year%d  Bob= %d\n", k, Bob );
         if ( i/12==k) {   // результат выводится по прошедствию года
              printf ("                                  Year%d  Bob's Capital= %d\n", k, Bob );
             k =k+1;
-        }
-    }
-    k=1;
-    printf( "Alice's calculations \n");
-    for (int j=1; j<=time; j++) {
-        Alice = (b  + (income ) - (expenses2));
-        b=Alice;
-        if (j > 3*12) {
-            Alice = (b*1.08);
-        } else {
-            Alice = (b* 1.06);
-        }
+       // }
+   // }
+   // k=1;
+    //printf( "Alice's calculations \n");
+    //for (int j=1; j<=time; j++) {
+      //  Alice = (b  + (income ) - (expenses2));
+       // b=Alice;
+       // if (j > 3*12) {
+       //     Alice = (b*1.08);
+       // } else {
+        //    Alice = (b* 1.06);
+      //  }
        // printf (" Alice= %d\n", b );
-        if (j / 12 == k) {   // результат выводится по прошедствию года
-            printf("                                  Year%d  Alice's Capital= %d\n", k, Alice);
-            k = k + 1;
-            flat = flat *1.05; // каждый год квартира дорожает на 5%
-        }
-        if (Alice>= flat and f==0  ) {
-            f=k;
-        }
-    }
-    if (Bob >0){
-        printf ("Bob paid off the mortgage \n");
-    }
-    printf ("Alice bought an apartment for %d years \n", f);
-    printf ("Bob's Final Capital= %d\n", Bob);
-    printf ("Alice's Final Capitale= %d\n", Alice );
+       // if (j / 12 == k) {   // результат выводится по прошедствию года
+       //     printf("                                  Year%d  Alice's Capital= %d\n", k, Alice);
+       //     k = k + 1;
+        //    flat = flat *1.05; // каждый год квартира дорожает на 5%
+       // }
+       // if (Alice>= flat and f==0  ) {
+        //    f=k;
+      //  }
+   // }
+   // if (Bob >0){
+      //  printf ("Bob paid off the mortgage \n");
+   // }
+   // printf ("Alice bought an apartment for %d years \n", f);
+   // printf ("Bob's Final Capital= %d\n", Bob);
+   // printf ("Alice's Final Capitale= %d\n", Alice );
+
     return 0;
 }
 
